# Base URL for the backend API (see API CONTRACT). Defaults to "/api" if unset,
# which works when the frontend is proxied to the same origin as the API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
